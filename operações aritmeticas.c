#include <stdio.h>
#include <locale.h>
void main(){
    setlocale(LC_ALL,"Portuguese");
   // int a = 7;
   // int b = 2;
   // int r = a   b;
   //printf("O resultado é %i",r);

   //float n1 = 8;
   //float n2 = 7;
   //float m = (n1 + n2) / 2;
   //printf("A média é %.2f", m);

   //float preco = 123.45;
   //float desc = preco * 10 / 100;
   //float total = preco - (preco * 10 / 100);
   //float aumento = preco + (preco * 10 / 100);
   //printf("O valor do desconto é: %.2f\n",desc);
   //printf("O preço total com o desconto é de: %.2f\n", total);
   //printf("O preço total com o aumento é de: %.2f",aumento);


   //char l = 'J';
   //l = l + 1;
   //printf("Eu estou com a letra %c na memoria", l); //(dá para calcular letras tbm, o resultado nesse caso será K)

}
